import { PageHeader } from '../../components/ui/Page'

// PLACEHOLDER — replaced during this build. Nothing here ships.
export function FinderPage() {
  return <PageHeader title="FinderPage" meta="Under construction." />
}
